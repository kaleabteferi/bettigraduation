# dependencies
/node_modules

# raw source photos/videos (the used copies live in /public)
/images

# media now lives in Supabase Storage (see MEDIA in src/lib/constants.ts),
# kept out of git so the repo stays small and pushes stay fast. Local copies
# remain on disk as a backup but are not committed.
/public/images
/public/videos

# local tooling — not part of the deployed app
/.claude
/.agents
/.codex

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel
.env*
